<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= scheme.title %> | Medical Schemes</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f4f6f9;
      margin: 0;
      padding: 20px;
    }
    .container {
      max-width: 800px;
      margin: auto;
      background: #fff;
      border-radius: 12px;
      padding: 30px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }
    h1 {
      color: #2c3e50;
      margin-bottom: 20px;
    }
    p {
      font-size: 15px;
      color: #555;
      margin-bottom: 10px;
    }
    .meta {
      font-size: 14px;
      color: #333;
      margin: 5px 0;
    }
    .benefit {
      font-weight: bold;
      color: #27ae60;
      margin: 15px 0;
    }
    .btn {
      display: inline-block;
      padding: 8px 14px;
      background: #2980b9;
      color: #fff;
      text-decoration: none;
      border-radius: 6px;
      font-size: 14px;
      transition: background 0.2s ease;
      margin-top: 15px;
    }
    .btn:hover {
      background: #1f6391;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1><%= scheme.title %></h1>
    <p><%= scheme.description %></p>
    <p class="meta"><strong>Category:</strong> <%= scheme.category || "N/A" %></p>
    <p class="meta"><strong>Eligibility:</strong> <%= scheme.eligibility || "N/A" %></p>
    <p class="meta"><strong>Location:</strong> <%= scheme.location || "Pan India" %></p>
    <p class="meta"><strong>Country:</strong> <%= scheme.country || "India" %></p>
    <p class="benefit">Benefit: <%= scheme.benefitAmount || "N/A" %></p>

    <% if (scheme.infoLink) { %>
      <a class="btn" href="<%= scheme.infoLink %>" target="_blank">Official Info</a>
    <% } %>

    <a class="btn" href="/schemes">← Back to All Schemes</a>
  </div>
</body>
</html>
